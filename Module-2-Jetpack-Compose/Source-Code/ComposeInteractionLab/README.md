# Compose Interaction Lab

A standalone Module 2 Android app demonstrating Compose events, input and observable state through a dice roller and tip calculator.

## Run

Open this folder in Android Studio, allow Gradle sync to finish, select a USB-debugging-enabled device, and run the `app` configuration.

## Verify

```powershell
.\gradlew.bat testDebugUnitTest
.\gradlew.bat assembleDebug assembleDebugAndroidTest
```
